# @parel/search-tools

Workspace text search tools for PAREL coding agents.

The plugin consumes the canonical `workspace` capability plus a lower-level
`exec` capability. It runs read-only search commands from the workspace root and
returns workspace-relative matches.
