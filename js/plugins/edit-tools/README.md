# @parel/edit-tools

Workspace exact-edit tools for PAREL coding agents.

The plugin consumes the canonical `workspace` capability plus a lower-level
`filesystem` capability. It performs exact text replacement against
workspace-relative files and rejects ambiguous edits by default.
