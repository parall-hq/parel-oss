# @parel/port-tools

Sandbox port tools for PAREL coding agents.

The plugin consumes a lower-level `ports` capability supplied by a sandbox
provider. It exposes provider URLs for dev servers and supports list/revoke.
