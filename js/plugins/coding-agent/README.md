# @parel/coding-agent

Coding agent profile for PAREL.

This plugin injects a system prompt profile for coding-agent behavior. It does
not install tools by itself; declare the workspace, filesystem, search, edit,
git, shell, process, port, approval, and subagent plugins explicitly in
`agent.yaml`.
