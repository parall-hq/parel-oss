# @parel/git-tools

Workspace Git tools for PAREL coding agents.

The plugin consumes the canonical `workspace` capability plus a lower-level
`exec` capability. The initial tools are read-only: status and diff.
