---
"@parel/cli": patch
---

`parel chat` renders slash-command outcomes (`command_result`), returns to the prompt after an inline command (`message_ack.status: "executed"`), and surfaces `unknown_command` warnings.
