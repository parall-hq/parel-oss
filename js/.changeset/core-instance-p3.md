---
"@parel/core": minor
---

RuntimeConfig gains `instanceBudgetUsd` — an instance-level spend ceiling in USD enforced by the platform at turn admission (one-turn lag). Public agent-config schema documents it alongside the new instance vars (`${var:NAME}`) substitution described in protocol/agent-config.md.
