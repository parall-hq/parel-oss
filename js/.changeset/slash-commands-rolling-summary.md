---
"@parel/memory-rolling-summary": minor
---

`/compact [focus]`: fold older history into the running summary on demand, regardless of the budget threshold. An optional focus text steers what the summary keeps. The automatic fold at step/turn end and the command share one implementation.
